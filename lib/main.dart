import 'package:flutter/material.dart';

import 'sections/build_context.dart';
import 'sections/decomposition/decomposition.dart';
import 'sections/global_key.dart';
import 'sections/keyed_subtree.dart';
import 'sections/keys.dart';
import 'sections/navigation.dart';
import 'sections/render_object_creation.dart';
import 'sections/render_object_using.dart';
import 'sections/stateless.dart';

void main() {
  runApp(const BuildContextApp());
  // runApp(const KeysApp());
  // runApp(const KeyedSubtreeApp());
  // runApp(const GlobalKeyApp());
  // runApp(const StatelessApp());
  // runApp(const DecompositionApp());
  // runApp(const RenderObjectCreationApp());
  // runApp(const RenderObjectUsingApp());
  // runApp(const NavigationApp());
}
